name: Docker Image CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-push-arm64:
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
    env:
      shim_version: 0.6.0
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Login to GitHub container registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Generate commit tag
      run: echo "GITHUB_SHA_SHORT=$(git rev-parse --short=6 '${{ github.sha }}')" >> $GITHUB_ENV

    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        context: ./image
        platforms: linux/amd64,linux/arm64
        push: true
        tags: |
          ghcr.io/${{ github.repository }}:${{ env.shim_version }}
          ghcr.io/${{ github.repository }}:${{ env.shim_version }}-${{ env.GITHUB_SHA_SHORT }}
        args: --build-arg SHIM_VERSION=${{ env.shim_version }}

    - name: clear docker creds
      if: always()
      run: |
        rm -f ${HOME}/.docker/config.json